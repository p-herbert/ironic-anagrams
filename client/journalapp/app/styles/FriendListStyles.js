import {
  StyleSheet,
  Dimensions,
} from 'react-native';

const styles = StyleSheet.create({
  subHeader: {
    fontSize: 12,
    fontWeight: '700',
    color:"#424242",
    marginLeft:12,
    marginBottom:12,
    marginTop:17
  },
  innerContainer: {
    borderRadius: 10,
    alignItems: 'center',
  }
});

module.exports = styles;